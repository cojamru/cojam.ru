doctype html
html
	head
		include inc/vars.pug
		include inc/mixins.pug
		- var subTitle = 'Музыка'

		meta(charset='utf-8')
		+title(title.full, subTitle)

		link(rel='canonical' href=`${URLs.same}/🎼`)

		include inc/meta.pug

		each URL in ['bandcamp.com', 'w.soundcloud.com']
			link(rel='dns-prefetch' href=`//${URL}`)

		+includeStyles(['parsers'])
	body
		include inc/header.pug

		main
			div.text
				p В этом разделе можно найти все наши музыкальные альбомы.
				p Синглы здесь не публикуются, но их можно найти на #[a(href='https://soundcloud.com/midipidemy/tracks' target='_blank') SoundCloud] или в #[a(href='https://vk.com/audios-71897180' target='_blank') VK].
			hr
			div.music

		include inc/footer.pug

		+includeScripts(['special/edge', 'db/music', 'parsers'])

		script document.addEventListener('DOMContentLoaded', () => $parser.music(musicDB, $make.qs('.music')))
